/** Inline guidance leaves the first card available to read and use. */
export function GestureHint() {
  return <p className="mt-2 mb-4 text-center text-xs text-[var(--color-text-secondary)] md:hidden">Swipe right to pin a note.</p>;
}
